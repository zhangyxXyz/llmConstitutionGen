---
name: project-info-generator
description: 项目信息生成器。扫描当前项目结构、技术栈、公共组件和工具函数,生成标准化的项目文档到 d2c skill 的 references 目录,并同步更新 d2c SKILL.md 中的关键章节。当用户需要 (1) 为新项目初始化 d2c 配置,(2) 更新项目技术栈信息,(3) 生成公共资源清单时使用此技能。一般只在新项目接入 d2c 或项目结构重大变更时使用一次。
---

# 项目信息生成器

## 🎯 核心定位

**项目上下文的静态分析器**,自动识别项目结构、公共资源、技术栈,生成标准化的项目文档,**为 D2C 提供项目特定的上下文信息**。

## 🚀 执行流程

### 阶段一:扫描项目信息

#### 步骤 1:识别项目结构

1. **读取根目录 README.md**
   ```bash
   read_file [项目根目录]/README.md
   ```

2. **读取 package.json**
   ```bash
   read_file [项目根目录]/package.json
   ```
   提取:
   - 项目名称
   - 技术栈版本(Vue、Vite、TypeScript 等)
   - 依赖包列表

3. **读取项目规范文档**
   ```bash
   read_file .codebuddy/rules/constitution.md
   read_file .codebuddy/rules/project-guide.md
   read_file .codebuddy/rules/modules/components.md
   ```

#### 步骤 2:扫描公共资源

1. **扫描公共组件**
   ```bash
   list_files packages/components/ --depth 2
   ```
   对于每个组件目录:
   - 读取 index.vue 源码
   - 提取 Props 定义
   - 提取 Events 定义
   - 记录导入路径

2. **扫描工具函数**
   ```bash
   list_files packages/utils/src/ --depth 2
   ```
   记录工具函数名称和路径

3. **扫描公共资源**
   ```bash
   list_files packages/assets/ --depth 2
   ```
   记录图片、字体、样式等资源路径

### 阶段二:生成项目文档

#### 输出路径

所有文档输出到:
```
.codebuddy/skills/create/d2c/references/
```

#### 必须生成的文档

| 文档名 | 用途 | 内容 |
|-------|------|------|
| `README.md` | 文件索引 | 所有文档的目录和用途说明 |
| `project-overview.md` | 项目概述 | 技术栈版本、路径别名、项目结构 |
| `component-templates.md` | 组件清单 | 公共组件详细文档(Props/Events/示例) |
| `code-generation-guide.md` | 代码规范 | Vue 3 代码生成规范和最佳实践 |

#### 可选生成的文档

根据项目特点生成:
- `amount-formatting-guide.md` - 金额格式化规范(有金额显示)
- `image-resource-guide.md` - 图片资源处理规范
- `download-button-template.md` - DownloadButton 组件模板(有文件下载)

### 阶段三:同步更新 D2C SKILL.md

#### 需要更新的章节

| D2C SKILL.md 章节 | 更新内容 | 触发条件 |
|------------------|---------|---------|
| **技术栈** | 版本号、框架、组件库 | 版本变更时 |
| **项目类型判断** | 项目目录、端口、依赖 | 项目结构变更时 |
| **资源查找优先级** | 公共组件、工具函数清单 | 新增公共资源时 |
| **快速参考** | 常用导入、路径别名、色值 | 配置变更时 |

#### 更新方法

1. 读取当前 D2C SKILL.md
   ```bash
   read_file .codebuddy/skills/create/d2c/SKILL.md
   ```

2. 使用 `replace_in_file` 更新对应章节
   ```bash
   replace_in_file(
     filePath: ".codebuddy/skills/create/d2c/SKILL.md",
     old_str: "[原技术栈章节内容]",
     new_str: "[新技术栈章节内容]"
   )
   ```

### 阶段四:验证输出

生成完成后,检查:
- ✅ 所有必需文档已生成
- ✅ 文档内容完整准确
- ✅ D2C SKILL.md 已更新
- ✅ 组件 Props/Events 提取完整

---

## 📋 组件文档模板

生成组件文档时,使用以下格式:

```markdown
### [组件名称]

| 属性 | 值 |
|-----|---|
| **源码路径** | `packages/components/[ComponentName]/index.vue` |
| **导入方式** | `import [ComponentName] from '@yka/components/[ComponentName]/index.vue'` |
| **内部依赖** | 列出依赖的其他组件 |

#### Props

| 属性名 | 类型 | 必填 | 默认值 | 说明 |
|-------|------|-----|-------|------|
| ... | ... | ... | ... | ... |

#### Events

| 事件名 | 参数 | 说明 |
|-------|-----|------|
| ... | ... | ... |

#### 使用示例

\`\`\`vue
<template>
  <ComponentName :prop1="value1" @event1="handler" />
</template>
\`\`\`
```

---

## 🔴 重要注意事项

### 执行时机

⚠️ **一般只使用一次**,仅在以下情况执行:
1. 新项目首次接入 d2c skill
2. 项目技术栈发生重大变更
3. 新增大量公共组件或模块

### 执行前确认

执行前必须询问用户:
```
检测到这是 [项目名称] 项目,将生成项目信息到 d2c skill。

将执行以下操作:
1. 扫描项目结构和公共资源
2. 生成文档到 .codebuddy/skills/create/d2c/references/
3. 更新 d2c SKILL.md 中的关键章节

是否继续?(回复"是"或"继续")
```

### 避免重复执行

如果 `.codebuddy/skills/create/d2c/references/` 目录已存在且文件较新,提示用户:
```
检测到已存在项目信息文档(更新时间:[日期])。

如果项目结构未变化,无需重新生成。是否仍要继续?
```

---

## 📝 技术详解(供维护者参考)

### D2C 协同方案技术架构

这是一个基于 **三层分离架构** 的 Design to Code 解决方案:

```
┌─────────────────────────────────────────────────────────────────┐
│                     D2C 协同架构                                  │
└─────────────────────────────────────────────────────────────────┘

  ┌──────────────────┐      ┌──────────────────┐      ┌──────────────────┐
  │  数据提取层       │      │  项目分析层       │      │  代码生成层       │
  │                  │      │                  │      │                  │
  │ figma-data-     │ ───▶ │ project-info-   │ ───▶ │      d2c        │
  │  extractor      │      │  generator      │      │                  │
  │                  │      │                  │      │                  │
  │ 设计稿数据       │      │ 项目上下文       │      │ 代码实现         │
  └──────────────────┘      └──────────────────┘      └──────────────────┘
         ↓                          ↓                          ↓
    Figma API              项目静态扫描                   Vue 3 代码
    本地脚本                文档生成                      Tailwind CSS
```

### 静态代码扫描原理

**核心思想**:通过扫描项目文件系统和配置文件,提取项目信息。

扫描流程:
1. 识别项目规范文档
   - `.codebuddy/rules/constitution.md` - 项目宪章
   - `.codebuddy/rules/project-guide.md` - 项目指南
   - `.codebuddy/rules/modules/components.md` - 组件规范

2. 识别 package.json
   - 根目录 package.json
   - 子项目 package.json(Monorepo)

3. 扫描公共资源
   - `packages/components/` - 公共组件
   - `packages/utils/` - 工具函数
   - `packages/assets/` - 公共资源

### 组件源码解析方法

**技术难点**:不仅要找到组件文件,还要提取组件的 Props、Events、内部依赖。

**解决方案**:读取组件源码,使用正则/AST 解析提取:
- Props 定义: `interface Props {}`
- Events 定义: `defineEmits<{}>()`
- 内部依赖: `import` 语句

### 输出到 D2C references 目录的设计

**设计原则**:项目信息直接输出到 D2C skill 的 references 目录,避免跨目录引用。

**优势**:
- ✅ D2C skill 直接读取本地文件,无需跨 skill 通信
- ✅ 文档按场景拆分,D2C 按需加载(避免上下文浪费)
- ✅ 支持版本管理和持续更新
